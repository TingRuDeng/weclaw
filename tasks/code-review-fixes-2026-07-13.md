# 2026-07-13 深度审查问题修复

## 目标

- 校准 `docs/CODE_REVIEW_2026-07-13.md` 中的事实、严重度和适用边界。
- 按风险顺序修复任务生命周期、附件去重、消息排队和并发安全问题。
- 为每个行为变更补充可复现的自动化测试。

## 非目标

- 不整体拆分 `messaging.Handler`。
- 不修改 Agent 协议、会话绑定规则和 `/new` 语义。
- 不引入自动建会话、静默回退或假成功路径。

## 当前事实

- 基准提交为 `9b42cda`，`main` 已与 `origin/main` 同步。
- 当前工作区已有 `docs/README.md`、`tasks/lessons.md` 和审查报告相关改动，必须保留。
- watcher、任务排队和 `/guide` 回滚共享 `task_state.go`，相关写操作必须串行。

## 决策日志

- watcher 采用明确状态分类，不把所有非终态无条件结束。
- 飞书附件去重采用有所有权的处理状态，不在失败时无条件删除共享 key。
- 飞书卡片选择先登记同窗口顺序屏障；卡片只表示已提交，业务成功以切换结果为准。
- Codex 会话绑定使用稳定 binding key 串行化，不能依赖切换前后会变化的 conversation ID。
- 任务启动与排队在同一临界区完成，返回结构化结果。
- 文件名按 UTF-8 字符边界控制字节预算，不按固定 rune 数量截断。

## 执行计划

- [x] P0 串行：校准审查报告与文档索引。
- [x] P1 串行：修复 Codex watcher 非终态生命周期。
- [x] P2 可并行：修复飞书附件失败反馈与去重状态。
- [x] P2A 串行：修复飞书卡片会话切换与普通消息抢跑竞态。
- [x] P3 串行：原子化 Codex、Claude 的任务启动与消息排队。
- [x] P4 串行：修复微信广播 `ClientID` 并发访问。
- [x] P5 可并行：修复文本去重、UTF-8 文件名和保存目录反馈。
- [x] P6 串行：修复 `/guide` 失败回滚窗口。
- [x] P7 串行：执行完整验证并通过 Review Gate。

## 进度记录

- 2026-07-13：用户确认现状、功能点、风险决策和文件级执行计划，HARD-GATE 已通过。
- 2026-07-13：P0 完成；修正审查统计、缺陷边界、架构结论和安全结论，文档校验通过。
- 2026-07-13：P1 完成；rollout 错误与 turn 替换进入失败终态，显式停止会清理任务并续跑 pending，临时 Desktop 断线保持活动。
- 2026-07-13：用户确认把卡片切换竞态并入当前修复；因与 P2 共同修改飞书事件入口，改为串行整合。
- 2026-07-13：P2/P2A 完成；附件失败可重试、预约所有权、永久资源错误、卡片顺序屏障和回调 context 生命周期均有回归测试。
- 2026-07-13：P3-P6 完成；补齐 `/new`、`/cwd` 绑定锁，统一 Claude 广播准入，修复停止态重连、文本判重维度和 `/guide` 发送中占位。
- 2026-07-13：并行 Review Gate 发现的高、中风险项已整合修复；飞书卡片命令设置两分钟执行上限，超时后释放同窗口队列。

## 验证结果

- P1：`go test ./messaging -count=1 -timeout 60s` 通过。
- P1：关键 watcher 用例 `go test -race ./messaging ... -timeout 60s` 通过。
- 全仓：`go test ./... -count=1 -timeout 120s` 通过。
- 竞态：`go test -race ./agent ./cmd ./feishu ./messaging -count=1 -timeout 90s` 通过。
- 静态检查：`go vet ./...`、`staticcheck ./...` 通过。
- 文档与差异：`python3 scripts/validate_docs.py . --profile generic`、`git diff --check` 通过。

## Review 小结

- 已核对 Spec、并发锁序、状态终态、异常反馈、测试覆盖和文件规模约束，未发现阻塞交付的问题。
- 飞书 adapter 的进程内顺序队列不会跨进程恢复；这是现有事件接入模型的剩余边界，不影响本次同进程会话切换竞态修复。
